## ofxProgressBar

Simple addon for a visual progress bar. Takes a pointer to the changing value so you don't have to worry about updating it. Can be used with static or dynamic maximum value (for queues and other progress bars of shifting duration). See the example for usage.